language: cpp

os:
  - linux
  - osx

dist: trusty
sudo: required

compiler: clang

jdk:
  - openjdk8

mono:
  - latest

install:
  - which java
  - java -version
  - echo $JAVA_HOME
  - source build/SetupCI.sh

script:
  - mono --version
  - ./build.sh -t Travis -v diagnostic
